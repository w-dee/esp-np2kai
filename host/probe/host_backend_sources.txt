# Project-owned Ubuntu/headless host backend inventory.
# Paths are repository-relative and must remain explicit; do not use globs.
# This inventory is intentionally empty until a separately reviewed host
# backend implementation phase adds its first translation unit.

# Firmware

This directory is reserved for the future ESP32-P4 firmware application. The
ESP-IDF application, CMake files, board configuration, and component
dependencies are intentionally not created in this initial setup.

The eventual firmware should keep board-specific code outside the emulator
core and retain a headless mode for emulator-core and integration tests.

